Research the Swoosh Elixir email library (current stable, ~1.17+/1.18+) and answer these specific questions precisely, citing doc sections. This is research only — do not write code. Use the svelte docs tools? NO — ignore that; fetch Swoosh docs from hexdocs.pm/swoosh (e.g. https://hexdocs.pm/swoosh/Swoosh.html, adapters pages, Swoosh.Mailer, Swoosh.TestAssertions, Swoosh.Adapters.Loops, Swoosh.Adapters.Mailpit, Swoosh.ApiClient.Finch, Swoosh.Adapters.Test) and websearch if needed.

Questions:
1. Swoosh.Adapters.Loops (the Loops.so adapter): exact module name, required config keys (api_key?), and exactly how it maps a %Swoosh.Email{} to the Loops POST /api/v1/transactional JSON payload. Specifically: where do `transactionalId` and `dataVariables` come from (subject? headers? provider_options? something else)? Show the exact payload-building code or documented example.
2. Swoosh.Adapters.Mailpit: config options (base_url/default port), what HTTP endpoint it hits, and what fields of %Swoosh.Email{} it sends (does it deliver real HTML/text bodies?). Version in which it was introduced.
3. Using Swoosh.ApiClient.Finch as the global api client: exact config lines (config :swoosh, :api_client, Swoosh.ApiClient.Finch) and the supervision requirement — what Finch child spec must be started, and what the default Finch process name is for Swoosh (is it `Swoosh.Finch`?), including how to use a custom named Finch instance via config (:finch_name? :default_finch_name?). Which Swoosh version made Finch the default/recommended api client, and what happens if no api_client is configured (fallback hackney?).
4. Swoosh.Mailer: defining a mailer module via `use Swoosh.Mailer, otp_app: :my_app` — the exact config schema under `config :my_app, MyApp.Mailer` (keys: adapter, api_key, base_url, endpoint/relay?, etc.), and whether config is read at compile time or runtime (can I override the adapter per-environment purely via config files? can tests override it via Application.put_env at runtime?).
5. Swoosh.TestAssertions: how to wire Swoosh.Adapters.Test in tests (via mailer config adapter: Swoosh.Adapters.Test?), the assertions API (assert_email_sent/1,2 with %Swoosh.Email{} or keyword patterns, refute_email_sent), whether it stores emails globally (per-node, works with async tests?), and any setup/cleanup needed (Swoosh.TestAssertions.clear_emails?).
6. Error shapes: when an api_client-based adapter gets a non-2xx response, what exactly does deliver/2 return (e.g. {:error, {status, body}}?)? And on network failure (connection refused), what error term surfaces through a Swoosh.Mailer.deliver call (a Swoosh.ApiClientError struct? a Req/Mint exception wrapped?). I need to classify 4xx vs 429 vs 5xx vs network errors reliably.
7. Custom headers on %Swoosh.Email{} (e.g. Email.header("idempotency-key", value)): supported? Do api_client-based adapters pass arbitrary custom headers through to the provider HTTP request?
8. Does Swoosh depend on :gen_smtp at all anymore (for SMTP adapter only?), and is gen_smtp optional? (We want to remove gen_smtp entirely and use Mailpit over HTTP.)
9. Minimum Elixir/OTP requirements for recent Swoosh (we're on whatever .mise.toml pins, likely Elixir 1.17/1.18+).

Return a compact structured answer per question with exact code snippets/config examples and source URLs.
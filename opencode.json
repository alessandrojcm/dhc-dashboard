{
  "$schema": "https://opencode.ai/config.json",
  "autoupdate": true,
  "lsp": false,
  "mcp": {
    "servers": {
      "sentry": {
        "type": "remote",
        "url": "https://mcp.sentry.dev/mcp",
        "disabled": false,
        "oauth": {}
      },
      "svelte": {
        "type": "remote",
        "url": "https://mcp.svelte.dev/mcp",
        "disabled": false
      }
    }
  },
  "permissions": [
    { "action": "sentry_*", "resource": "*", "effect": "deny" }
  ],
  "agents": {
    "triaging": {
      "description": "Triage Sentry production issues using the build agent workflow with Sentry MCP tools enabled.",
      "mode": "primary",
      "system": "Inherit and follow the same build-task implementation prompt and project instructions as the build agent. You are additionally responsible for triaging Sentry production issues: inspect Sentry issues, events, traces, breadcrumbs, and Seer analysis when relevant; summarize impact, root cause, and recommended code fixes; and only make code changes when the user asks for implementation.",
      "permissions": [
        { "action": "sentry_*", "resource": "*", "effect": "allow" }
      ]
    }
  }
}
